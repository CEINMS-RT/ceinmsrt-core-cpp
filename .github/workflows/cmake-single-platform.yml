# This starter workflow is for a CMake project running on a single platform. There is a different starter workflow if you need cross-platform coverage.
# See: https://github.com/actions/starter-workflows/blob/main/ci/cmake-multi-platform.yml
name: CMake on a single platform

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  # Customize the CMake build type here (Release, Debug, RelWithDebInfo, etc.)
  BUILD_TYPE: Debug

jobs:
  build:
    # The CMake configure and build commands are platform agnostic and should work equally well on Windows or Mac.
    # You can convert this to a matrix build if you need cross-platform coverage.
    # See: https://docs.github.com/en/free-pro-team@latest/actions/learn-github-actions/managing-complex-workflows#using-a-build-matrix
    # 
    # IMPORTANT: This workflow uses a self-hosted runner with pre-installed dependencies.
    # For security reasons, it only runs on pull requests from the same repository.
    # Required dependencies on the self-hosted runner:
    #   - Boost 1.65.0+
    #   - XSD (XML Schema to C++ data binding compiler)
    #   - Xerces-C (XML parser)
    #   - Qt5 (Core, OpenGL, Gui, PrintSupport, Widgets)
    #   - OpenGL and GLEW
    #   - OpenSim
    #   - CMake 3.10+
    #   - Git (for submodules)
    runs-on: self-hosted
    # Only run on pull requests from the same repository (not from forks) for security
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository)

    steps:
    - name: Cleanup workspace
      run: |
        # Remove any leftover build artifacts from previous runs
        if [ -d "${{github.workspace}}/build" ]; then
          rm -rf "${{github.workspace}}/build"
        fi
    
    - uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Configure CMake
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: cmake -B ${{github.workspace}}/build -DCMAKE_BUILD_TYPE=${{env.BUILD_TYPE}}

    - name: Build
      # Build your program with the given configuration
      run: cmake --build ${{github.workspace}}/build --config ${{env.BUILD_TYPE}}

    - name: Test
      working-directory: ${{github.workspace}}/build
      # Execute tests defined by the CMake configuration.
      # See https://cmake.org/cmake/help/latest/manual/ctest.1.html for more detail
      run: ctest -C ${{env.BUILD_TYPE}}

    - name: Cleanup build artifacts
      if: always()
      run: |
        # Clean up build artifacts to free disk space on self-hosted runner
        if [ -d "${{github.workspace}}/build" ]; then
          rm -rf "${{github.workspace}}/build"
        fi
